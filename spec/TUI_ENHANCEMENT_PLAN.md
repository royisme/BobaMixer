# TUI Enhancement Plan - 功能迁移计划

## 目标

将所有交互式CLI命令迁移到TUI中，使BobaMixer成为一个真正的TUI优先应用。

## 当前TUI状态

已有的TUI视图（来自 `internal/ui/tui.go`）：
- ✅ `ViewDashboard` - 概览页面
- ✅ `ViewProfiles` - Profile管理（有导航和选择功能）
- ✅ `ViewBudget` - 预算状态显示
- ✅ `ViewTrends` - 趋势分析
- ✅ `ViewSessions` - 会话历史

## 需要添加的TUI视图

### Phase 1: Control Plane核心功能

#### 1. Providers视图
**当前CLI命令**: `boba providers`

**TUI实现**:
```
┌─ Providers Management ─────────────────────────────────┐
│                                                         │
│  ▶ OpenAI          (api.openai.com)       ✓ Active     │
│    Anthropic       (api.anthropic.com)                 │
│    Azure OpenAI    (custom endpoint)                   │
│    Google AI       (generativelanguage.g...)           │
│                                                         │
│  [a] Add Provider  [e] Edit  [d] Delete  [t] Test     │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 列表显示所有providers（读取 providers.yaml）
- 上下键导航
- `a` 键打开添加表单
- `e` 键编辑选中的provider
- `d` 键删除（需要确认）
- `t` 键测试连接

#### 2. Tools视图
**当前CLI命令**: `boba tools`

**TUI实现**:
```
┌─ CLI Tools Management ─────────────────────────────────┐
│                                                         │
│  ▶ aichat          /usr/local/bin/aichat   ✓ Detected │
│    claude          /usr/local/bin/claude               │
│    chatgpt-cli     /usr/bin/chatgpt                    │
│                                                         │
│  [a] Add Tool  [e] Edit Path  [r] Refresh Detection   │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 显示检测到的和手动添加的工具
- `a` 键手动添加工具
- `e` 键编辑工具路径
- `r` 键重新检测工具

#### 3. Bindings视图
**当前CLI命令**: `boba bind <tool> <provider>`

**TUI实现**:
```
┌─ Tool ↔ Provider Bindings ────────────────────────────┐
│                                                         │
│  Tool           Provider      Proxy    Status          │
│  ────────────────────────────────────────────────────  │
│  ▶ aichat      OpenAI         ON      ✓ Active        │
│    claude      Anthropic      OFF     ✓ Active        │
│    chatgpt     OpenAI         ON      ⚠ No API Key    │
│                                                         │
│  [n] New Binding  [e] Edit  [d] Delete  [p] Toggle Proxy │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 显示所有绑定关系
- `n` 键创建新绑定（打开表单：选择tool → 选择provider → 配置proxy）
- `e` 键编辑绑定
- `d` 键删除绑定
- `p` 键切换proxy开关

#### 4. Secrets视图
**当前CLI命令**: `boba secrets [list|set|remove]`

**TUI实现**:
```
┌─ Secrets Management (API Keys) ────────────────────────┐
│                                                         │
│  Provider         Status          Last Updated         │
│  ────────────────────────────────────────────────────  │
│  ▶ OpenAI        ✓ Configured    2025-11-15 10:30     │
│    Anthropic     ✗ Missing       -                     │
│    Azure         ✓ Configured    2025-11-10 14:20     │
│    Google AI     ✗ Missing       -                     │
│                                                         │
│  [s] Set API Key  [r] Remove  [t] Test Connection     │
│                                                         │
│  🔒 Secrets are encrypted and stored in ~/.boba/secrets.yaml │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- **安全列表显示**：不显示实际密钥，只显示状态
- `s` 键设置密钥（打开密码输入框，masked input）
- `r` 键删除密钥（需要确认）
- `t` 键测试API连接
- **优势**：TUI中输入密钥不会出现在shell历史中，更安全！

#### 5. Proxy视图
**当前CLI命令**: `boba proxy serve`, `boba proxy status`

**TUI实现**:
```
┌─ Proxy Server Control ─────────────────────────────────┐
│                                                         │
│  Status:    ● Running                                  │
│  Address:   http://localhost:8080                      │
│  PID:       12345                                      │
│  Uptime:    2h 15m                                     │
│                                                         │
│  Recent Requests (Last 10):                            │
│  ────────────────────────────────────────────────────  │
│  15:30:25  POST /v1/chat  → OpenAI     200  450ms     │
│  15:29:12  POST /v1/chat  → Anthropic  200  320ms     │
│  15:28:45  POST /v1/chat  → OpenAI     200  380ms     │
│                                                         │
│  [s] Start  [x] Stop  [r] Restart  [l] View Logs      │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 实时显示proxy状态
- `s` 键启动proxy
- `x` 键停止proxy
- `r` 键重启proxy
- `l` 键查看详细日志
- 自动刷新状态和请求列表

### Phase 2: 高级功能

#### 6. Routing Rules视图
**当前CLI命令**: `boba route test <text>`

**TUI实现**:
```
┌─ Routing Rules Tester ─────────────────────────────────┐
│                                                         │
│  Input Text:                                           │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Write a Python function to parse JSON             │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  Context:                                              │
│  • Project: BobaMixer (go, web)                       │
│  • Branch: main                                        │
│  • Time: evening                                       │
│  • Length: 42 chars                                    │
│                                                         │
│  Routing Decision:                                     │
│  → Profile: claude-sonnet-3.5                          │
│  → Rule: short-query-fast-model                        │
│  → Reason: Query < 100 chars, use fast model          │
│                                                         │
│  [Enter] Test  [e] Edit Rules  [c] Clear              │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 文本输入框（支持多行）
- 实时显示context信息
- 显示routing决策过程
- `Enter` 键测试routing
- `e` 键编辑routing规则（打开YAML编辑器）
- `c` 键清空输入

#### 7. Git Hooks视图
**当前CLI命令**: `boba hooks install|remove|track`

**TUI实现**:
```
┌─ Git Hooks Management ─────────────────────────────────┐
│                                                         │
│  Current Repository: /Users/you/code/BobaMixer         │
│  Git Hooks Status:   ✓ Installed                      │
│                                                         │
│  Hook Events:                                          │
│  ────────────────────────────────────────────────────  │
│  pre-commit        ✓ Active                            │
│  post-commit       ✓ Active                            │
│  pre-push          ✗ Inactive                          │
│                                                         │
│  Recent Hook Activity:                                 │
│  ────────────────────────────────────────────────────  │
│  15:30  pre-commit  BobaMixer  main    ✓ Success      │
│  14:25  post-commit BobaMixer  main    ✓ Success      │
│  13:15  pre-commit  BobaMixer  feat/x  ✓ Success      │
│                                                         │
│  [i] Install  [u] Uninstall  [r] Reload               │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 显示当前repo的hooks状态
- `i` 键安装hooks到当前repo
- `u` 键卸载hooks
- `r` 键刷新状态
- 显示最近的hook事件

#### 8. Reports Generator视图
**当前CLI命令**: `boba report [--format json|csv] [--out file]`

**TUI实现**:
```
┌─ Generate Usage Report ────────────────────────────────┐
│                                                         │
│  Time Range:                                           │
│    ○ Last 7 days   ● Last 30 days   ○ Custom range    │
│                                                         │
│  Output Format:                                        │
│    ● JSON   ○ CSV   ○ HTML                            │
│                                                         │
│  Include:                                              │
│    [✓] Summary statistics                              │
│    [✓] Daily trends                                    │
│    [✓] Profile breakdown                               │
│    [✓] Cost analysis                                   │
│    [✓] Optimization suggestions                        │
│                                                         │
│  Output File:                                          │
│  ┌───────────────────────────────────────────────────┐ │
│  │ ~/reports/bobamixer-2025-11-18.json               │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  [g] Generate Report  [p] Preview  [c] Cancel         │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 交互式选择报告选项
- `g` 键生成报告
- `p` 键预览报告内容
- 支持自定义时间范围
- 显示生成进度

#### 9. Suggestions/Actions视图
**当前CLI命令**: `boba action [--auto]`, `boba suggest`

**TUI实现**:
```
┌─ Optimization Suggestions ─────────────────────────────┐
│                                                         │
│  Suggestions (Last 7 days):                            │
│  ────────────────────────────────────────────────────  │
│  ▶ [P3] Switch to cheaper model for simple queries     │
│     Impact: Save ~$12/month                            │
│     Action: Update routing rules for <50 char queries │
│     [Space] Apply                                      │
│                                                         │
│    [P2] Reduce token usage in profile 'default'        │
│     Impact: Save ~$8/month                             │
│     Action: Lower max_tokens from 2048 to 1024        │
│     [Space] Apply                                      │
│                                                         │
│    [P1] Enable caching for repeated requests           │
│     Impact: Save ~$5/month                             │
│     Action: Enable response caching in proxy           │
│     [Space] Apply                                      │
│                                                         │
│  [Space] Toggle  [a] Apply All  [r] Refresh            │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 显示优化建议列表（带优先级）
- Space键勾选/取消建议
- `a` 键应用所有高优先级建议
- `r` 键刷新建议
- 显示预估节省金额

### Phase 3: 配置编辑器

#### 10. Config Editor视图
**当前CLI命令**: `boba edit <target>`

**TUI实现**:
```
┌─ Configuration Editor ─────────────────────────────────┐
│                                                         │
│  Select Configuration:                                 │
│                                                         │
│  ▶ Providers  (providers.yaml)                         │
│    Tools      (tools.yaml)                             │
│    Bindings   (bindings.yaml)                          │
│    Secrets    (secrets.yaml)                           │
│    Routes     (routes.yaml)                            │
│    Pricing    (pricing.yaml)                           │
│    Settings   (settings.yaml)                          │
│                                                         │
│  [Enter] Edit in $EDITOR  [v] View  [ESC] Back        │
└─────────────────────────────────────────────────────────┘
```

**功能**:
- 列表显示所有配置文件
- `Enter` 键在外部编辑器中打开
- `v` 键在TUI内查看（只读）
- 显示最后修改时间

## TUI导航改进

### 增强的Tab导航

当前只有5个视图，需要扩展到更多：

```
Tab顺序:
[1] Dashboard     - 概览
[2] Providers     - Provider管理
[3] Tools         - Tool管理
[4] Bindings      - 绑定管理
[5] Secrets       - 密钥管理
[6] Proxy         - 代理控制
[7] Profiles      - Profile管理（已有）
[8] Budget        - 预算状态（已有）
[9] Stats         - 统计数据（已有）
[10] Trends       - 趋势分析（已有）
[11] Sessions     - 会话历史（已有）
[12] Routing      - 路由测试
[13] Hooks        - Git hooks
[14] Suggestions  - 优化建议
[15] Reports      - 报告生成
[16] Config       - 配置编辑
```

### 快捷键设计

全局快捷键：
- `Tab` / `Shift+Tab` - 切换视图
- `q` / `Ctrl+C` - 退出
- `?` - 显示帮助
- `r` - 刷新当前视图
- `/` - 搜索/过滤
- `ESC` - 返回上级/取消

数字快捷键：
- `1` - Dashboard
- `2` - Providers
- `3` - Tools
- `4` - Bindings
- ...

## 表单组件设计

需要实现的TUI组件：

### 1. Input框（文本输入）
```go
type TextInput struct {
    Label       string
    Placeholder string
    Value       string
    Masked      bool  // 用于密码
    Validation  func(string) error
}
```

### 2. Select框（单选）
```go
type Select struct {
    Label   string
    Options []string
    Selected int
}
```

### 3. MultiSelect框（多选）
```go
type MultiSelect struct {
    Label   string
    Options []string
    Selected map[int]bool
}
```

### 4. Form（组合表单）
```go
type Form struct {
    Title  string
    Fields []Field
    OnSubmit func(values map[string]interface{}) error
}
```

### 5. Confirm Dialog（确认对话框）
```go
type Confirm struct {
    Title   string
    Message string
    Default bool
}
```

## 实施优先级

### Sprint 1: 核心Control Plane视图
1. ✅ 重构Help输出
2. ⏳ Providers视图
3. ⏳ Tools视图
4. ⏳ Bindings视图
5. ⏳ Secrets视图（**最重要**）

### Sprint 2: 运营功能
6. ⏳ Proxy视图
7. ⏳ Routing测试器
8. ⏳ Suggestions视图

### Sprint 3: 高级功能
9. ⏳ Reports生成器
10. ⏳ Hooks管理
11. ⏳ Config编辑器

### Sprint 4: TUI优化
12. ⏳ 增强导航系统
13. ⏳ 添加搜索/过滤
14. ⏳ 改进主题和样式
15. ⏳ 添加快捷键帮助页面

## 技术实现

### 使用的Bubble Tea组件

基础组件：
- `github.com/charmbracelet/bubbles/textinput` - 文本输入
- `github.com/charmbracelet/bubbles/list` - 列表选择
- `github.com/charmbracelet/bubbles/table` - 表格显示
- `github.com/charmbracelet/bubbles/viewport` - 滚动视图
- `github.com/charmbracelet/bubbles/progress` - 进度条
- `github.com/charmbracelet/bubbles/spinner` - 加载动画

样式：
- `github.com/charmbracelet/lipgloss` - 样式和布局

### 架构模式

采用Bubble Tea的标准模式：

```go
type ProviderView struct {
    list     list.Model
    providers []Provider
    // ... 其他状态
}

func (v ProviderView) Init() tea.Cmd
func (v ProviderView) Update(msg tea.Msg) (tea.Model, tea.Cmd)
func (v ProviderView) View() string
```

每个视图是独立的Bubble Tea model，由主Model管理切换。

## 迁移路径

### 阶段1：并存（当前）
- CLI命令继续工作
- 逐步添加TUI视图
- 用户可以选择使用CLI或TUI

### 阶段2：TUI优先
- TUI功能完整
- CLI命令标记为deprecated
- 文档推荐使用TUI

### 阶段3：纯TUI
- 移除大部分CLI命令
- 只保留必要的非交互式命令
- 达到Bubble Tea最佳实践标准

## 成功标准

完成后，BobaMixer应该：
1. ✅ 默认启动进入功能完整的TUI
2. ✅ 99%的交互操作在TUI中完成
3. ✅ CLI只保留非交互式操作
4. ✅ 符合Bubble Tea应用最佳实践
5. ✅ 用户体验一致且直观
6. ✅ 密钥管理更安全（TUI内输入）
7. ✅ 易于发现功能（不需要记住CLI命令）
