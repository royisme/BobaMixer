# BobaMixer Profiles Configuration
#
# This file defines AI provider profiles for routing requests.
# Each profile specifies a model provider, endpoint, and parameters.
#
# Quick Start:
#   1. Add your API key to secrets.yaml (e.g., anthropic: "sk-ant-...")
#   2. Modify the profiles below to match your setup
#   3. Run: boba chat "hello"
#
# For more examples, see: configs/examples/profiles.yaml

profiles:
  # Default profile - ready to use with Anthropic Claude
  default:
    name: "Default Profile"
    adapter: "http"
    provider: "anthropic"
    endpoint: "https://api.anthropic.com"
    model: "claude-3-5-sonnet-latest"
    max_tokens: 4096
    temperature: 0.7
    env:
      ANTHROPIC_API_KEY: "secret://anthropic"

  # Uncomment and configure additional profiles as needed:

  # quick-tasks:
  #   name: "Quick Tasks"
  #   adapter: "http"
  #   provider: "openrouter"
  #   endpoint: "https://openrouter.ai/api/v1"
  #   model: "deepseek/deepseek-chat"
  #   max_tokens: 2048
  #   temperature: 0.3
  #   env:
  #     OPENROUTER_API_KEY: "secret://openrouter"
